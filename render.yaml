services:
  - type: web
    name: fir-document-intelligence
    runtime: python
    plan: free

    buildCommand: bash render-build.sh

    startCommand: streamlit run streamlit_app.py --server.port $PORT --server.address 0.0.0.0

    envVars:
      - key: GROQ_API_KEY
        sync: false
